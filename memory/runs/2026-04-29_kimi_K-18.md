---
name: 2026-04-29_kimi_K-18
type: run
---

## Goal
K-18: DB-Engine lazy init via `@lru_cache` — Engine nicht mehr beim Modul-Import erstellen.

## Done
- `database.py`: `get_engine()` mit `@lru_cache(maxsize=1)` eingeführt; `get_session_factory()` statt globaler `AsyncSessionLocal`; `reset_db()` Test-Helper hinzugefuegt
- `conftest.py`: `get_engine.cache_clear()` nach env-Setzung; `reset_db()` statt manuellem drop_all+init_db
- `audit.py` + `decision_tree.py`: `AsyncSessionLocal` → `get_session_factory()` migriert
- 6/6 pytest gruen

## Surprised by
`AsyncSessionLocal` wurde in 2 Route-Files importiert (`audit.py`, `decision_tree.py`). Entfernen aus `database.py` brach diese sofort. Lesson: Lazy-Refactor immer mit `grep` auf alle Consumer checken.

## Avoided
Settings werden nicht mehr beim Modul-Import gecacht (B-08), sondern erst bei `get_engine()`-Aufruf.

## Next
K-19 (Cornerstone3D WASM-Init) parallel zu K-18 geplant — jetzt starten.

## Memory updates
- `tasks.jsonl`: K-18 auf `done` setzen
- `09b_KIMI_PROMPT_SEQUENCE.md`: K-18 Prompt ergaenzen — `AsyncSessionLocal`-Consumer in Route-Files mitrefactoren
