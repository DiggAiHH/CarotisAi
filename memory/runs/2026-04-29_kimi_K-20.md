---
name: 2026-04-29_kimi_K-20
type: run
---

## Goal
K-20: 10 synthetische anonymisierte DICOMs + 10 Decision-Tree-JSONs fuer Demo.

## Done
- `code/scripts/generate_demo_data.py`: 512x512 Synthetic CT-DICOMs mit Carotis-Bifurkations-Phantom + Schema-valide Decision-Trees
- `code/scripts/test_generate_demo_data.py`: 5 Tests (10 DICOMs, 0 PII, anonymize.py check, 10 Trees, Schema-Validation)
- `model_sha` auf SHA256-Hash umgestellt (passt zu Schema-Regex `^[a-f0-9]{7,64}$`)
- 5/5 pytest gruen

## Surprised by
1. `validate_decision_tree.py` hat Unicode-Pfeile (`✓`, `✗`) die auf Windows cp1252 crashen — B-13 trifft auch bestehende Skripte.
2. `anonymize.py --check` braucht `--output` + `--min-k` (k-Anonymity-Default 5 ist zu hoch fuer 10 Demo-Dateien).

## Avoided
Keine Unicode-Pfeile in `generate_demo_data.py` (B-13 befolgt).

## Next
K-21 (5-Min-Demo-Walkthrough) + K-22 (Dashboard-Update) parallel.

## Memory updates
- `tasks.jsonl`: K-20 auf `done` setzen
- `09b_KIMI_PROMPT_SEQUENCE.md`: K-20 — `model_sha` muss Hex-Regex matchen; `anonymize.py --check` braucht `--output` + `--min-k 1`
