---
name: 2026-04-29_kimi_K-17
type: run
---

## Goal
K-17: Router-Prefix `/api/v1` für alle Domain-Router + pytest-asyncio Config-Fix.

## Done
- `main.py`: inference, decision_tree, audit mit `prefix="/api/v1"` versehen; health bleibt root
- `test_smoke.py`: 4 Pfade auf `/api/v1/*` umgestellt (predict, capture, recent)
- `pytest.ini`: `asyncio_default_fixture_loop_scope = function`, `filterwarnings`, `addopts` ergänzt
- `apiClient.ts`: `API_PREFIX = "/api/v1"` eingeführt; predict + captureDecisionTree nutzen ihn
- 6/6 pytest grün, TypeScript typecheck 0 errors

## Surprised by
Router-Prefixe in den Route-Files (`/inference`, `/decision-tree`, `/audit`) waren bereits korrekt gesetzt — `main.py` brauchte nur den äußeren `/api/v1`-Wrapper. Keine Refactoring-Notwendigkeit in den Einzeldateien.

## Avoided
Korrigiere den K-17-Prompt-Text: "Entferne Prefixe aus den Route-Files" ist falsch — sie müssen bleiben, sonst würde `/api/v1` + `/predict` = `/api/v1/predict` statt `/api/v1/inference/predict` ergeben.

## Next
K-18 (DB-Engine lazy init) + K-19 (Cornerstone3D WASM) parallel starten.

## Memory updates
- `tasks.jsonl`: K-17 auf `done` setzen
- `09b_KIMI_PROMPT_SEQUENCE.md`: K-17 Prompt korrigieren — Route-File-Prefixe NICHT entfernen
